#!/bin/bash

bold=$(tput bold)
normal=$(tput sgr0)

echo -e;
echo "${bold}NPM${normal}"; echo -e;
echo "Installing latest npm version..."; echo -e;
npm install -g npm@latest; echo -e;

echo "${bold}NPM - PACKAGES${normal}"; echo -e;
echo "Updating...";
npm update -g
